import React from 'react'

export const OrderSummaryBanner = (props) => {
    return (
        <div id = "OrderSummaryBanner">
            OrderSummaryBanner
            {props.children}
            
        </div>
    )
}
