import React from 'react'

export const OrderSummaryChangeLink = (props) => {
    return (
        <div id="OrderSummaryChangeLink">
            {props.children}
            Link
        </div>
    )
}
