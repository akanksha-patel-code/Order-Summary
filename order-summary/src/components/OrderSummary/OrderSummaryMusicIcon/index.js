import React from 'react'

export const OrderSummaryMusicIcon = (props) => {
    return (
        <div id="OrderSummaryMusicIcon">
            {props.children}
            Icon
        </div>
    )
}
