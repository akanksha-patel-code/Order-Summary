import React from 'react'

export const OrderSummaryWrapper = (props) => {
    return (
        <div id ="OrderSummaryWrapper">
             OrderSummaryWrapper
             { props.children }
        </div>
    )
}
